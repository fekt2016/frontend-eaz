export const metadata = {
  title: "Account",
  description: "Sign in or create your EazWorld account.",
  robots: { index: false, follow: true },
};

export default function AuthLayout({ children }) {
  return children;
}
